A build of the scala-archetype-simple maven archetype, with added support for eclipse.

When you run "mvn eclipse:eclipse" this adds the scala-nature to the project.

TODO
add the scala files to the build path.
