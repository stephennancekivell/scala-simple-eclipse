A build of the scala-archetype-simple maven archetype, with added support for eclipse.

When you run "mvn eclipse:eclipse" this adds the scala-nature and the source files to the eclipse project.

TODO
Fix eclipse. Even though the scala nature has been added, when trying to run scala files, they cant find the scala library.
